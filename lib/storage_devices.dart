import 'package:flutter/material.dart';

class StorageDevicesPage extends StatelessWidget {
  const StorageDevicesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text(
      "Storage Devices goes here",
      style: TextStyle(
        color: Colors.white,
      ),
    );
  }
}
