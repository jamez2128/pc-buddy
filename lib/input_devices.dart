import 'package:flutter/material.dart';

class InputDevicesPage extends StatelessWidget {
  const InputDevicesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text(
      "Input Devices goes here",
      style: TextStyle(
        color: Colors.white,
      ),
    );
  }
}
