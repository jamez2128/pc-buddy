import 'package:flutter/material.dart';

class ComputerCasePage extends StatelessWidget {
  const ComputerCasePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text(
      "Computer Case goes here",
      style: TextStyle(
        color: Colors.white,
      ),
    );
  }
}
