import 'package:flutter/material.dart';

class OperatingSystemsPage extends StatelessWidget {
  const OperatingSystemsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text(
      "Operating Systems goes here",
      style: TextStyle(
        color: Colors.white,
      ),
    );
  }
}
